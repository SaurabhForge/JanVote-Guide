// Application configuration

export const config = {
  // GCP API URL for the JanVote Guide backend/services
  apiUrl: process.env.NEXT_PUBLIC_API_URL || 'https://janvote-guide-444951391032.asia-south1.run.app',
};

export default config;
